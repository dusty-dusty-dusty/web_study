
$(function(){
   
  /* var topDiv =$('.tabSet');
   var anchors = topDiv.find('ul.tabs > li> a'); 
   var panelDivs =topDiv.find('.panel');
   
   anchors.show();
   panelDivs.hide();
   
   var lastAnchor = anchors.filter('.on') //finc /filter
   var  lastPanel = $(lastAnchor.attr('href')); 
   console.log(lastPanel);
   lastPanel.show();
      
   anchors.click(function(){
      var currentAnchor = $(this); //눌려진 이벤트가 발생한 변수 설정
      var currentPanel =$(currentAnchor.attr('href'));
      
      lastAnchor.removeClass('on');
      currentAnchor.addClass('on');
      
      
      lastPanel.hide();
      currentPanel.show();
      
      lastPanel =currentPanel;
      lastAnchor = currentAnchor;
   })*/

   $('.tabSet').tabs
   
   
   
})
   
   
   
   
   
   
   
   
   
   
   
   
